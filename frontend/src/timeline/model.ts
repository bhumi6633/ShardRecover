import{pathEdges}from'../forensics/model';import type{Trace}from'../trace/types'
export type TimelineEvent={kind:'graph'|'path'|'join'|'repair'|'format';label:string;detail:string;edgeIndex?:number;repairIndex?:number}
export function timeline(trace:Trace):TimelineEvent[]{const events:TimelineEvent[]=[{kind:'graph',label:'Graph built',detail:`${trace.fragments.length} fragments · ${trace.edges.length} edges`},{kind:'path',label:'Path selected',detail:`${trace.selected_path.length} fragments · ${trace.reconstruction.complete?'Complete':'Incomplete'}`}];pathEdges(trace).forEach((edge,index)=>events.push({kind:'join',label:`JOIN ${String(index+1).padStart(2,'0')} · F${edge.from} → F${edge.to}`,detail:`${edge.overlap} B · ${edge.exact?'Exact':`Approximate · ${edge.mismatches} mismatches`}`,edgeIndex:index}));trace.repairs.forEach((repair,index)=>events.push({kind:'repair',label:`${repair.stage==='png_crc'?'PNG CRC':'Consensus'} repair`,detail:`Offset 0x${repair.position.toString(16).toUpperCase()} · ${repair.before} → ${repair.after}`,repairIndex:index}));if(trace.format.type!=='none')events.push({kind:'format',label:`${trace.format.type.toUpperCase()} analysis`,detail:`${trace.format.valid_crc_count} valid CRC · ${trace.format.invalid_crc_count} invalid`});return events}
